import styled from 'styled-components';

export const CSImgImg = styled.img`
  display: block;
  width: 100%;
  height: auto;
  grid-column: 4 / 10;
  box-shadow: 0 6px 30px 5px rgba(0, 0, 0, 0.22);
  border-radius: 13px;
`;
